# Astra Frontend

Vanilla HTML/CSS frontend with no npm dependencies.

Run locally

```bash
cd frontend
node public/server.js
```

App runs on port `3001` by default.

Structure
- `public/index.html` — main page (dark gradient theme)
- `public/server.js` — simple static file server
